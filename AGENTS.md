# Project notes

This repository no longer uses Next.js.

Current architecture:
- backend: Go server in `main.go`
- frontend: Vite + React app in `frontend/`
- production frontend assets: generated into `dist/`

When making UI changes, prefer reading and editing files under `frontend/src/`.
